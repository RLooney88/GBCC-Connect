rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic rules - you can customize these based on your app's security requirements
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 